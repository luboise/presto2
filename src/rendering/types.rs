/*
#[derive(Debug, Clone)]
pub struct BindError;

pub trait Bind<S> {
    fn bind(slot: S) -> Result<(), BindError>;
}

// impl Bind<u8> for Texture {}

pub trait Texture {
    fn write() -> Result<(), ()>;
}

pub struct
*/
